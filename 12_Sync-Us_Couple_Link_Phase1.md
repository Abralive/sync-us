# Sync-Us 第一件事：情侶如何互相連結

日期：2026-08-11  
目標：先把「兩個人如何安全、清楚、低摩擦地成為一組」定義完整。沒有配對，就不應該進入共享星域、共同泡泡、共同足跡或對方小手冊。

---

## 1. 產品原則

Sync-Us 的連結不是社群加好友，也不是聊天室邀請。

它應該像：

```text
我們要不要一起建立一本共同生活手冊？
```

所以流程要有三個重點：

1. 雙方都要同意。
2. 使用者要知道自己正在共享哪些內容。
3. 配對完成前，不可以看到對方的私人資料。

---

## 2. 建議的使用者流程

### A. 第一次使用

1. 使用者打開 Sync-Us。
2. 進入註冊 / 登入畫面。
3. 註冊自己的帳號。
4. 進入「建立連結」頁。
5. 系統詢問：

```text
你想和誰一起使用 Sync-Us？
```

提供兩種方式：

- 輸入對方 Email。
- 產生邀請碼 / 邀請連結。

---

### B. 使用 Email 邀請

適合：雙方都已經知道對方帳號 Email。

流程：

1. A 輸入 B 的 Email。
2. 系統建立一筆 `pending` 配對邀請。
3. B 登入後看到邀請。
4. B 可以選擇：
   - 接受
   - 拒絕
5. B 接受後，couple 狀態變成 `active`。
6. 雙方開始共享「共享泡泡」、「共同足跡」、「共同手冊」。

---

### C. 使用邀請碼 / 邀請連結

適合：要給男朋友快速設定。

流程：

1. A 點擊「產生邀請」。
2. 系統產生一組短碼，例如：

```text
SYNC-4821
```

3. A 把邀請碼傳給 B。
4. B 註冊或登入後輸入邀請碼。
5. 系統顯示：

```text
你要和 A 建立 Sync-Us 連結嗎？
```

6. B 確認後完成配對。

邀請碼應該有期限，例如 24 小時或 7 天，避免被長期濫用。

---

## 3. 手機版畫面架構

### 尚未配對時

底部導覽可以先保留，但「星域」首頁應顯示空狀態：

```text
還沒有共享星域
先邀請對方一起建立你們的泡泡宇宙。

[邀請對方]
[輸入邀請碼]
```

不要顯示假的 Mina / Kai / 測試資料。

---

### 建立邀請畫面

畫面內容要簡單：

```text
建立你們的連結

方法一
[輸入對方 Email]
[送出邀請]

方法二
[產生邀請碼]
SYNC-4821
[複製邀請碼]
```

重點是降低摩擦，不要做成複雜表單。

---

### 收到邀請畫面

```text
A 想和你建立 Sync-Us 連結

連結後，你們可以共享：
- 共享泡泡
- 共同完成紀錄
- 已確認的共同手冊資料

不會共享：
- 私人泡泡
- 未確認的手冊建議
- 你沒有填寫或確認的敏感資料

[接受連結]
[拒絕]
```

---

## 4. 資料庫設計

目前已有 `users` 與 `couples`，但要讓連結流程更完整，建議補一張邀請表。

### users

| 欄位 | 說明 |
|---|---|
| id | 使用者 ID |
| username | 顯示名稱 |
| email | 登入與邀請使用 |
| avatar_url | 頭像，沒有就用預設圖 |
| created_at | 建立時間 |

### couples

| 欄位 | 說明 |
|---|---|
| id | 配對 ID |
| partner_a_id | 伴侶 A |
| partner_b_id | 伴侶 B |
| status | `active` / `ended` |
| created_at | 建立時間 |
| ended_at | 解除時間 |

### couple_invites

| 欄位 | 說明 |
|---|---|
| id | 邀請 ID |
| inviter_id | 發起邀請者 |
| invitee_email | 被邀請者 Email，可為空 |
| invite_code | 邀請碼 |
| status | `pending` / `accepted` / `declined` / `expired` / `cancelled` |
| expires_at | 過期時間 |
| accepted_by_id | 接受者 |
| couple_id | 接受後產生的 couple |
| created_at | 建立時間 |
| updated_at | 更新時間 |

---

## 5. API 設計

### 建立邀請

```http
POST /api/v1/couple-invites
```

Request:

```json
{
  "inviter_id": 1,
  "invitee_email": "partner@example.com"
}
```

Response:

```json
{
  "id": 12,
  "invite_code": "SYNC-4821",
  "status": "pending",
  "expires_at": "2026-08-12T21:00:00"
}
```

---

### 查詢我的邀請

```http
GET /api/v1/couple-invites/user/{user_id}
```

用途：

- 顯示我送出的邀請。
- 顯示我收到的邀請。
- 顯示是否已經有待接受邀請。

---

### 接受邀請

```http
POST /api/v1/couple-invites/{invite_code}/accept
```

Request:

```json
{
  "user_id": 2
}
```

成功後：

1. 建立或啟用 `couples`。
2. 邀請狀態改為 `accepted`。
3. 回傳 couple 資料。

---

### 拒絕邀請

```http
POST /api/v1/couple-invites/{invite_code}/decline
```

---

## 6. 權限規則

這裡不能只靠前端隱藏。

後端必須檢查：

1. 一個使用者同時間只能有一個 `active` couple。
2. 只有邀請的發起者可以取消邀請。
3. 只有被邀請者或輸入正確邀請碼的人可以接受邀請。
4. 已過期邀請不能接受。
5. 已接受邀請不能重複接受。
6. 配對解除後，不可再讀取該 pair 的共享資料。
7. 私人泡泡永遠不因配對而公開。

---

## 7. 與現有功能的關係

### 星域

未配對：

- 顯示建立連結入口。
- 不顯示共享泡泡。

已配對：

- 顯示共享星域。
- 顯示雙方共享泡泡。
- 私人泡泡只顯示給建立者。

---

### 泡泡

未配對：

- 可以建立私人泡泡。
- 建立共享泡泡時提示先建立連結。

已配對：

- 可以建立共享泡泡。
- 可以分配參與者。
- 完成共享泡泡後進入共同足跡。

---

### 連結

未配對：

- 顯示邀請對方 / 輸入邀請碼。

已配對：

- 顯示對方的小手冊。
- 顯示共同足跡。
- 顯示已確認資料查詢。

---

## 8. 錯誤與空狀態

### 尚未配對

```text
你們還沒有建立連結
先邀請對方，Sync-Us 才能知道哪些泡泡屬於你們兩個。
```

### 對方還沒接受

```text
邀請已送出
等對方接受後，你們的共享星域就會打開。
```

### 邀請碼過期

```text
這組邀請碼已過期
請重新產生一組新的邀請。
```

### 已經有配對

```text
你已經有一個 Sync-Us 連結
若要更換，需要先解除目前連結。
```

---

## 9. 第一階段驗收條件

完成「情侶連結」至少要通過這些檢查：

- [ ] 新使用者註冊後，如果沒有 couple，會看到建立連結入口。
- [ ] 使用者可以產生邀請碼。
- [ ] 另一位使用者可以輸入邀請碼。
- [ ] 接受邀請後，雙方都能取得同一個 couple_id。
- [ ] 接受後星域、泡泡、連結頁都切換成已配對狀態。
- [ ] 還沒配對時不能建立共享泡泡。
- [ ] 私人泡泡不會被對方看到。
- [ ] 過期邀請不能使用。
- [ ] 同一個邀請不能重複接受。
- [ ] 使用者不能讀取不屬於自己的 couple 資料。

---

## 10. 今天建議實作順序

1. 補 `couple_invites` 資料表。
2. 補建立邀請、查詢邀請、接受邀請、拒絕邀請 API。
3. 修改前端未配對狀態，不再顯示假資料。
4. 在「連結」頁加入邀請對方與輸入邀請碼流程。
5. 泡泡建立時檢查是否已配對，未配對只能建立私人泡泡。
6. 完成 6 個驗收測試。
7. 再進入第二階段：手機與網域試用。

---

## 11. 結論

Sync-Us 的第一個核心不是「做任務」，而是：

```text
兩個人正式建立一個共同空間。
```

這個共同空間一旦成立，後面的共享泡泡、共同足跡、星塵商城、對方小手冊才有產品意義。

---

## 12. 目前實作狀態

已完成：

- [x] 新增 `couple_invites` 資料表。
- [x] 新增邀請碼建立 API。
- [x] 新增依使用者查詢邀請 API。
- [x] 新增依邀請碼查詢 API。
- [x] 新增接受邀請 API。
- [x] 新增拒絕邀請 API。
- [x] 接受邀請後自動建立 `active` couple。
- [x] 防止使用者接受自己的邀請。
- [x] 防止 Email 指定邀請被其他帳號接受。
- [x] 防止同一邀請重複接受。
- [x] 防止已配對使用者再建立第二組 active couple。
- [x] 未配對使用者登入後優先進入「連結」頁。
- [x] 前端「連結」頁支援產生邀請碼、複製邀請碼、輸入邀請碼接受。
- [x] 手機版已可看到邀請流程。

已驗證：

- [x] `python tests\verify_couple_invites.py`
- [x] `python tests\verify_connection_features.py`
- [x] `npm.cmd run build`
- [x] `http://127.0.0.1:8051/api/v1/health`

下一步：

- [ ] 用你和男朋友的真實帳號跑一次完整流程。
- [ ] 若要放到網域，確認 `syncus.hunghung.xyz` tunnel 能連到 `localhost:8051`。
- [ ] 若要上架或長期使用，把 SQLite 換成正式雲端資料庫。
